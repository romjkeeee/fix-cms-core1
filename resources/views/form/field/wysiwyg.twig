{% extends 'admin::form.field._layout' %}
{% block input %}
    {#<div class="card-box">#}

    {% filter html_inline %}
        <textarea class="tinymce-elem cms-field-tinymce" id="{{ id }}" name="{{ name }}"

        {% if options.rows %}rows="{{ options.rows }}"{% endif %}
        {% if options.height %}data-height="{{ options.height }}"{% endif %}
        {% if readonly %}readonly{% endif %}
        {% if disabled %}disabled{% endif %}

        {% if required %}
            data-bv-notempty="true"
            data-bv-notempty-message='{{ trans('admin::form.validation.not_empty', {label: options.label}) }}'
        {% endif %}

        >{% endfilter %}{{ old(name, value) }}</textarea>

    {#</div>#}

    {% if options.help %}
        <span class="help-block"><small>{{ options.help|nl2br }}</small></span>
    {% endif %}
{% endblock %}
